# Server
PORT=3001
SERVER_URL=http://localhost:3001

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=your_password
DB_NAME=ticketing

# JWT
JWT_SECRET=supersecret
JWT_EXPIRE=7d

# Email (use ONE of the following setups)
# 1) Gmail (requires App Password if 2FA is enabled):
# EMAIL_SERVICE=gmail
# EMAIL_USER=your@gmail.com
# EMAIL_PASS=your_app_password

# 2) Generic SMTP (e.g., Mailtrap, your SMTP)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=example@example.com
EMAIL_PASS=your_password
EMAIL_FROM=CCIS Ticketing <noreply@ccis.edu.ph>

# 3) Development with no credentials: Ethereal will be used automatically
# No configuration needed; a preview link will be printed to the console.

# Test helper
TEST_EMAIL_TO=your_email@example.com
