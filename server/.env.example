NODE_ENV=development
PORT=3001
CLIENT_URL=http://localhost:5173

# Database Configuration
DB_DIALECT=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=upat_ticketing
DB_USER=root
DB_PASSWORD=
DB_SSL=false
# If using a provider that requires SSL (e.g., Aiven), set DB_SSL=true and provide a CA path
# Place your CA file under ./server/certs and point to it below
# Example: ./certs/ca.pem
DB_SSL_CA_PATH=
# Optional: set to false to relax certificate verification (use only if you cannot provide a CA)
DB_SSL_STRICT=true
# MySQL specific settings
# Note: maxAllowedPacket must be configured at the MySQL server level (my.cnf/my.ini)
# Example: max_allowed_packet=64M in your MySQL configuration
# DB_MAX_ALLOWED_PACKET is not used by the client - it's a server setting
DB_CONNECT_TIMEOUT=60000
DB_POOL_MAX=10
DB_POOL_MIN=0
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_make_it_very_long_and_secure
JWT_EXPIRE=7d

# Email Configuration (for ticket confirmations)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=devcommgio2006@gmail.com
EMAIL_PASS=xzrvgpklckcyhwaz
EMAIL_FROM=CCISTicketingSystem <noreply@ccis.edu.ph>


# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# QR Code Configuration
QR_CODE_SIZE=200
QR_CODE_ERROR_CORRECTION=M
